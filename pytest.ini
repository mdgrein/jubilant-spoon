[pytest]
asyncio_mode = auto
addopts = --basetemp=D:/development/pytest-tmp
markers =
    webui_contract: Tests that enforce the JSON contract between static/index.html and server/main.py. If you change these tests, you MUST update BOTH sides (JS and server) or the UI will break.
